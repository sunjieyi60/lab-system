spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/lab_sys4?useSSL=false&serverTimezone=UTC
    username: root
    password: sbx159357!?.
  data:
    redis:
      host: localhost
      port: 6379
      database: 2

server:
  port: 8083

t-io:
  server:
    name: tio-server
    host: "localhost"
    port: 9000
    heartbeat: 10000

# 设备认证配置
device:
  auth:
    server:
      # 服务端RSA私钥（Base64编码，用于解密设备信息）
      # 方式1：直接配置Base64字符串
      private-key: ""
      # 方式2：从文件读取（推荐，更安全）
      private-key-file: ""